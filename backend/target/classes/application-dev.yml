# 开发环境配置 (application-dev.yml)
# 激活方式: spring.profiles.active=dev

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inspirehub_dev

logging:
  level:
    com.inspirehub: DEBUG
    org.springframework.security: DEBUG
